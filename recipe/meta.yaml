{% set name = "historia" %}
{% set version = "0.10.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 330913bf14318ec86623f82480c1488d80b62f1e386bbdfb1428cae06cd4c479

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - historia = historia._cli:historia_cli

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - requests
    - tqdm
    - rich-click
    - beartype

test:
  requires:
    - python {{ python_min }}
    - pytest
  source_files:
    - pyproject.toml
    - tests
  imports:
    - historia
  commands:
    - pytest -m "not remote" -c pyproject.toml

about:
  home: https://github.com/codycbakerphd/historia
  summary: A command-line tool for backing up records of GitHub activity
  description: |
    A command-line tool for backing up records of GitHub activity for use in creating dashboard reports of work history.
  license: MIT
  license_file: LICENSE.txt
  doc_url: https://historia.readthedocs.io/
  dev_url: https://github.com/codycbakerphd/historia

extra:
  recipe-maintainers:
    - codycbakerphd
